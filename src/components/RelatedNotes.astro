---
import type { GardenNote } from '../lib/garden';
import StatusBadge from './StatusBadge.astro';
import { useTranslations, gardenNotePath, slugFromNoteId } from '../i18n/utils';
import type { Lang } from '../i18n/ui';

interface Props {
	notes: GardenNote[];
}

const { notes } = Astro.props;
const lang = (Astro.currentLocale || 'en') as Lang;
const t = useTranslations(lang);
---

{notes.length > 0 && (
	<section class="related-notes">
		<h2 class="related-title">{t('note.relatedNotes')}</h2>
		<ul class="related-list">
			{notes.map((note) => (
				<li class="related-item">
					<a href={gardenNotePath(lang, slugFromNoteId(note.id))} class="related-link">
						{note.data.title}
					</a>
					<StatusBadge status={note.data.status} size="sm" />
				</li>
			))}
		</ul>
	</section>
)}

<style>
	.related-notes {
		margin-top: 2rem;
		padding-top: 1.5rem;
		border-top: 1px solid var(--color-border);
	}

	.related-title {
		font-size: 0.85rem;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		color: var(--color-text-secondary);
		margin: 0 0 0.75rem 0;
	}

	.related-list {
		list-style: none;
		padding: 0;
		margin: 0;
		display: flex;
		flex-direction: column;
		gap: 0.35rem;
	}

	.related-item {
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}

	.related-link {
		color: var(--color-accent);
		text-decoration: none;
		font-size: 0.95rem;
	}

	.related-link:hover {
		text-decoration: underline;
	}
</style>
